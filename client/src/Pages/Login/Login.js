import React from "react";
import styles from "./login.module.scss";
import { Container } from "react-bootstrap";

const Login = () => {
  return (
    <div>
      <Container className={styles.container}>Login</Container>
    </div>
  );
};

export default Login;
