import React from "react";
import styles from "./register.module.scss";
import { Container } from "react-bootstrap";

const Register = () => {
  return (
    <div>
      <Container className={styles.container}>Register</Container>
    </div>
  );
};

export default Register;
